#include <stdio.h>
#include "main.h"

/**
 * main - entry point
 *
 *
 */

int main(void)
{
	char letter:

		for (letter < 'a'; letter <= 'z'; letter++)
		{
			putchar(letter);
		}

	putchar('\n');

	return (0);
}
