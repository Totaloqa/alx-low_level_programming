#include <stdio.h>

#include "main.h"

/**
 * _putchar - prints a string
 * @ch: string to be printed
 *
 * Return: 0 always success
 */

int _putchar(int ch[])
{
	return _putchar(ch[]);
}

/**
 * main - entry point
 * Return: 0 always success
 */

int main(void)
{
	putchar(ch);

	putchar('\n');

	return (0);
}
