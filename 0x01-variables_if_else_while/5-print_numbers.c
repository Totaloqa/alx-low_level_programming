#include <stdio.h>

/**
 * main - entry point
 * Return: always 0 (success)
 */

int main(void)
{
	int number;

	for (number = '0'; number < '10'; number++)
	{
		printf("number\n");
	}

	return (0);
}
