#include <stdio.h>

/**
 * main - entry point
 * return: 0 always (success)
 */

int main(void)
{
	char letter;

	for (letter = 'a'; letter <= 'z'; letter++);
	{
		putchar(letter);
	}

	putchar(\n);

	for (letter = 'A'; letter <= 'Z'; letter++);
	{
		putchar(letters);
	}

	return (0);
}
